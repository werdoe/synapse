# Creative Agent Reflection Template

Use for: Cinder, Alenta, Dalcenti

---

## Reflection — {HH:MM} — {N} — {deliverable or session name}

### What did I produce?
{brief description of the output — campaign, copy, research report, etc.}

### What was harder to get right?
{one specific element that required the most iteration or care — or "Straightforward" if nothing was difficult}

### What would I change on revision?
{one specific improvement for the next iteration — or "No changes" if this was your best effort}

### Pattern I'm noticing in my own work:
{one recurring strength or gap you're observing across multiple deliverables — or "No pattern yet"}

---

**Note:** Append to `memory/reflections/{agent}/YYYY-MM-DD.md`. The entry number `{N}` is the count of existing `## Reflection —` entries in that file + 1.

**Immutability:** Once your reflection entry is processed by synthesis, it cannot be edited. To correct an entry, append a new one with a new timestamp. Never edit a processed entry.