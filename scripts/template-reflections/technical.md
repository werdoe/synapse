# Technical Agent Reflection Template

Use for: Kilvin, Stercus, Bast

---

## Reflection — {HH:MM} — {N} — {task name}

### What did I do?
{concrete description of the work completed}

### What broke or surprised me?
{specific item that didn't work as expected — or "Nothing unexpected" if nothing did}

### What would I do differently?
{specific process or approach change — or "No changes" if nothing would change}

### Does this challenge any of my personal guidance?
{cite P-{agent}-XX if yes — or "No" if nothing challenges existing guidance}

### Flag for Kvothe:
{any pattern worth surfacing to the orchestrator — or "None" if nothing to flag}

---

**Note:** Append to `memory/reflections/{agent}/YYYY-MM-DD.md`. The entry number `{N}` is the count of existing `## Reflection —` entries in that file + 1. Use this to generate the stable dedupe ID: `{agent}::{open-date}::{N}::{HH:MM}`.

**Immutability:** Once your reflection entry is processed by synthesis, it cannot be edited. To correct an entry, append a new one with a new timestamp. Never edit a processed entry.